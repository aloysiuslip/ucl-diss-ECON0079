\section{Dynamic models}
\label{s:model-dynamic-panel}

I have discussed how the static model requires a strong strict exogeneity assumption.
I showed in subsection \ref{ss-static-identification} that
cross-sectional orthogonality is a \todo{necessary condition} to meet this assumption
with a peer-effects regressor.
\begin{align*}
    y_{i,t} &= \alpha_i + \gamma_t + \beta_0
                + \beta_1 k_{i,t}
                + \beta_2 l_{i,t}
                + \delta_0 \sum_{j\neq i} w_{ij}z_{j,t}
                + \varepsilon_{i,t}
    \\
    z_{i,t} &= \alpha_i + \gamma_t + \beta_0
                + \delta_0 \sum_{j\neq i} w_{ij}z_{j,t}
                + \varepsilon_{i,t}
    \\
    \mathbb E[\varepsilon_{i,t}\varepsilon_{j,t}] &= 0, \ \forall t
    \\
    \implies 
    \mathbb E[\varepsilon_{i,t}|x_{i,t}] &= 0, \ \forall t
    \qquad
    x_{i,t} =
    \begin{pmatrix}
        k_{i,t} &   l_{i,t} &   \sum_{j\neq i} w_{ij,t}z_{j,t}
    \end{pmatrix}
    \\
    \implies 
    \mathbb E[\Delta \varepsilon_{i,t}|\Delta x_{i,t}] &= 0, \ \forall t
\end{align*}
In this section, I introduce a dynamic panel specification to relax these assumptions,
permitting weak cross-sectional correlation between peer errors
and endogeneity of the contemperaneous regressors.
Crucially, this specification can still be identified even in its panel reduced form,
which requires first differences to eliminate fixed effects, by the use of internal instruments.
This follows econometric theory developed by \citep{Anderson1981} and estimator techniques
developed in \citep{Arellano1991} and furthered by \citep{Blundell1998} which improve efficiency.
\par
There are good reasons to think this weak cross-sectional correlation exists.
A local demand shock \todo{...}

\subsection{Instrumental identification under no error autocorrelation}

The estimator given in \citep{Arellano1991} takes the regression equation
as given above and allows for endogeneity.
Internal instruments of all the endogenous regressors are found,
meaning their lagged first differences.
Identification is improved in the System GMM approach given in \citep{Blundell1998}
by also including the first logged, not differenced, lag of the endogenous regressors
which meets the usual instrument condiitons, as an instrument.
It is this System GMM approach that I follow here,
so I find the first logged, not differenced, lag of regressor in $x_{it}$
and its corresponding set of first difference lagged instruments,
which in practice just start from $\Delta x_{i,\tau-j}$ for any $j$
if $x_{i,\tau}$ is a valid instrument.
\par
I commence finding these valid instruments by assuming the structural specification (\ref{eq:static-main-gva})
exhibits no autocorrelation in its error term $\varepsilon_{i,t}$,
though I now allow for moderate cross-sectional correlation across firms.
\begin{align}
    \label{eq:dyn-no-ac}
    \mathbb E[\varepsilon_{i,t}\varepsilon_{i,t-1}] &= 0       ,\qquad \forall t,t-1
    \\
    \label{eq:dyn-contemperaneous-endogeneity}
    \mathbb E[x_{i,t}\varepsilon_{i,t}] &\neq 0              ,\qquad \exists i
    \\
    \label{eq:dyn-xs-corr-errors}
    \mathbb E[\varepsilon_{i,t}\varepsilon_{j,t}] &\neq 0   ,\qquad \exists i,j
    \\
    \label{eq:dyn-error-wn}
    \mathbb E[\varepsilon_t]    =0,
    \mathbb E[\varepsilon_t^2]  =\sigma_t^2,
    \mathbb E[\varepsilon_t|\Omega_{t-1}] &=0                  ,\qquad \forall i
    \qquad\text{(w.n.)}
    \\ \notag
    x_{i,t} &=
    \begin{pmatrix}
        k_{i,t} &   l_{i,t} &   \sum_{j\neq i} w_{ij,t}z_{j,t}
    \end{pmatrix}
    \\ \notag
    \Delta x_{i,t} &=
    \begin{pmatrix}
        \Delta k_{i,t} &   \Delta l_{i,t} &   \Delta \sum_{j\neq i} w_{ij,t}z_{j,t}
    \end{pmatrix}
\end{align}

Taking each regressor in turn, I relax the exogeneity assumption
of the standard factors in the reduced-form equation, $\Delta k_{i,t}$ and $\Delta l_{i,t}$.
I assume a new optimal firm decision on capital based on information,
denoted by $\Omega_t$ which includes $\varepsilon_{i,t}$ for its respective $t$.
This approach allows the discovery of valid instruments which are exogenous to $\varepsilon_{i,t}$
and $\Delta\varepsilon_{i,t}$.
I justify an exclusion restriction for $\Delta k_{i,t-j}\ \forall j>1$, as it is predetermined,
and $\Delta l_{i,t-k}, k>2$, as it is flexible.
\begin{align}
    \Delta y_{i,t} &= \Delta \gamma_t
                + \beta_1\Delta k_{i,t}
                + \beta_2\Delta l_{i,t}
                + \delta_0\sum_{j\neq i} w_{ij}\Delta z_{j,t}
                + \Delta\varepsilon_{i,t}
    \\
    \label{eq:dyn-k-pim}
    K_{i,t} &= I_{i,t} + dK_{i,t-1}
    \\
    \label{eq:dyn-inv-information}
    I_{i,t} &= \mathbb{E}[I_{i,t}^* \mid \Omega_{t-1}]
    \\
    \label{eq:dyn-l-information}
    L_{i,t} &= \mathbb{E}[L_{i,t}^* \mid \Omega_t] 
    \\ \notag
    \varepsilon_{i,t} &\in \Omega_t
    \\ \notag
    &\implies K_{i,t} \propto \epsilon_{t-1}
    \\ \notag
    &\implies L_{i,t} \propto \epsilon_{t}
    \\ \notag
    \Delta \varepsilon_{i,t} &= \varepsilon_{i,t} - \varepsilon_{i,t-1}
    \\ \notag
    \Delta k_{i,t} &= k_{i,t} - k_{i,t-1}
        \propto \varepsilon_{i,t-1} \varepsilon_{i,t-2}
    \\ \notag
    &\implies \mathbb E[\Delta k_{i,t} \Delta \epsilon_t] \neq 0
    \\
    &\implies \mathbb E[\Delta k_{i,t-1} \Delta \epsilon_t] = 0, \mathbb E[k_{i,t-1} \varepsilon_t]=0
    \\ \notag
    \Delta l_{i,t} &= l_{i,t} - l_{i,t-1}
        \propto \varepsilon_{i,t}, \varepsilon_{i,t-1}
    \\
    &\implies \mathbb E[\Delta l_{i,t-2} \Delta \epsilon_t] = 0, \mathbb E[l_{i,t-2} \varepsilon_t]=0
\end{align}

Devising an internal instrument for $\Delta \sum_{j\neq i} w_{ij,t}z_{j,t}$ requires
a similar evaluation of the spatial econometric properties of $z_{j,t}$ as in the static problem.
$z_{it}$ is a linear combination of 
Importantly, the deep lag strategy instrumenting via $z_{i,t-2}$ is valid
even with this section's new cross-sectional correlation in equation \ref{eq:dyn-xs-corr-errors}.
\begin{align}
    \eqreref{eq:static-z-ma-inft}
    \mathbf z_{i,t} &= \sum_{k=0}^\infty \delta_0^k \mathbf W^k \cdot (\boldsymbol\alpha + \gamma_t \boldsymbol\iota + \beta_0\boldsymbol\iota + \boldsymbol\varepsilon_t)
    \\
    \Delta \mathbf z_t &= \sum_{k=0}^\infty \delta_0^k \mathbf W^k \cdot (\Delta \gamma_t \boldsymbol\iota + \boldsymbol\Delta\varepsilon_t)
\end{align}
\subsection{Autoregressive specification}

A dynamic specification grants the flexibility to add past realisations of variables
to the decomposition of TFP, $z_{i,t}$. This increases the number of regressors in reduced form,
improving the plausibility of its exogeneity with $\varepsilon_{i,t}$.
\begin{align}
    y_{i,t} &= \alpha_i + \gamma_t + \beta_0
                + \beta_1 k_{i,t}
                + \beta_2 l_{i,t}
                + \phi_1 y_{i,t-1}
                + \delta_0 \sum_{j\neq i} w_{ij}z_{j,t}
                + \varepsilon_{i,t}
\end{align}
The above equation is in structural form in the 1-stage regression,
but I can find an equivalent in the 2-stage regression with $z_{i,t}$ as the dependent variable.
\begin{align}
    z_{i,t} &= \alpha_i + \gamma_t + \beta_0
                + \phi_1 y_{i,t-1}
                + \delta_0 \sum_{j\neq i} w_{ij}z_{j,t}
                + \varepsilon_{i,t}
    \\
            &= \alpha_i + \gamma_t + \beta_0
                + \phi_1 \cdot (\beta_1 k_{i,t-1}, + \beta_2 l_{i,t-1} + z_{i,t-1})
                + \delta_0 \sum_{j\neq i} w_{ij}z_{j,t}
                + \varepsilon_{i,t}
\end{align}
For system GMM, I need to show that the reduced-form first difference regressors are exogenous.
\begin{align}
    \Delta y_{i,t} &= \Delta \gamma_t
                + \beta_1 \Delta k_{i,t}
                + \beta_2 \Delta l_{i,t}
                + \sum_{k=1}^p \phi_k \Delta y_{i,t-k}
                + \delta_0 \sum_{j\neq i} w_{ij,t} \Delta z_{j,t}
                + \Delta \varepsilon_{i,t}
    \\
    \Delta z_{i,t} &= \Delta\gamma_t
                + \phi_1 \cdot (\beta_1 \Delta k_{i,t-1}, + \beta_2 \Delta l_{i,t-1} + \Delta z_{i,t-1})
                + \delta_0 \sum_{j\neq i} w_{ij}\Delta z_{j,t}
                + \Delta \varepsilon_{i,t}
\end{align}

The first-order autoregressor, $\Delta ln(y_{it})$ is endogenous.
\begin{align}
    \Delta \varepsilon_{i,t} &= \varepsilon_{i,t} - \varepsilon_{i,t-1}
    \\ \notag
    \Delta y_{i,t-1} &= \qquad\dots\qquad + \Delta\varepsilon_{i,t-1}
    \\ \notag
    &= \qquad\dots\qquad + \varepsilon_{i,t-1} - \varepsilon_{i,t-2}
    \\ \notag
    &\implies \mathbb E[ \Delta y_{i,t-1}\Delta\varepsilon_{i,t}] \neq 0
    \\ \notag
    \Delta y_{i,t-2} &= \qquad\dots\qquad + \varepsilon_{i,t-2} - \varepsilon_{i,t-3}
    \\ \notag
    &\implies \mathbb E[ \Delta y_{i,t-1}\Delta\varepsilon_{i,t}] = 0
\end{align}

This is confirmed by the Anderson-Rubin (AR(1)) test in my results,
which rejects the null hypothesis at the 1\% level for all models,
suggesting that first-order lags of log GVA are a weak instrument
\todo{Why? Shouldn't they fail exclusion restriction, not instrument correlation}.
In addition, I assume that capital, $k_{i,t}$ is pre-determined, as investment is chosen
one period ahead, in line with standard Perpetual Inventory Method approaches\footnote{
    Noting that this is ordinarily done on a quarterly basis,
    however, due to data limitations we adopt the same thing here even though Fame
    provides observations annually.
}\todo{Review predetermined material. Additionally, explain the logic of instrumenting for capital}
\par
To overcome this endogeneity, I introduce deeper autoregressive lags as instrumental variables,
as proposed by \citep{Anderson1981}.
I use 3 autoregressive instrument lags starting from $t-2$:
$\ln(y_{i,t-2}), \ln(y_{i,t-2}), \ln(y_{i,t-p-3})$ for log GVA,
and 2 lags for capital $\ln(k_{i,t-p-1}), ln(k_{t-p-2})$.
An Anderson-Rubin (AR(2)) test shows that deep lags are a valid instrument for log GVA.
The test applied to the second lag of of $ln(gva_1)$
fails to reject the null hypothesis of for any specification,
so I assume it is a valid instrument.
\par
Additionally, using multiple deep lags allows for overidentification:
in this basic AR(1) specification, I have $k=4$ regressors and $l=7$ instruments.
I could increase this. Fame is rich on the time dimension, with up to 19 lags at most for a firm,
and a median of 6 lags, so I could safely provide more lags of regressors as instruments.
Further lags is likely to have a decreasing marginal effect on explanatory power
and additionally \todo(...)

\todo{Discuss introducing a new weighting method using past firm similarity.}
\par
I estimate the equation fulfilling the vector of moment conditions,
(\ref{eq:dyn-moment-conditions}) via GMM.
\begin{align}
    \label{eq:dyn-moment-conditions}
    \mathbb E \left[
        \Delta\varepsilon_{it}
        \cdot
        \begin{pmatrix}
            \Delta \ln(y_{i,t-2})     \\
            \Delta \ln(y_{i,t-3})     \\
            \Delta \ln(y_{i,t-4})     \\
            \Delta \ln(k_{i,t-2})     \\
            \Delta \ln(k_{i,t-3})     \\
            \Delta \Sigma_jw_{ijt}z_{jt}
        \end{pmatrix}
    \right]
    &= 0
\end{align}
As my regressors are over-identified, I use 2-step GMM to improve efficiency.
\todo{I should introduce a control (basically a diff-in-diff?) of peers from the same industry
across the country (outside the spatial bubble).}
 
\wraptable
    {Dynamic panel: all instrumented variables}
    {../../model/output/results_3e_2iv}
    {
        Model includes a constant as the baseline for \itx{t} fixed effects.
    }

\wraptable
    {Dynamic panel using instrumented continuous distance-decay weighted TFP}
    {../../model/output/results_3_ivdynamic}
    {
        Model includes a constant as the baseline for \itx{t} fixed effects.
    }

\par 
Finally, a Sargan-Hansen J-test is implemented to check if these instruments are 
over-identified, meaning the moment conditions cannot be fulfilled with 95\% confidence.
In all models, the J-test fails to reject the null hypothesis,
meaning the model's moment conditions are jointly correct.

\subsection{Instrumental identification under error autocorrelation}

I appear to have strong identification given assumptions (\ref{eq:dyn-no-ac}) and (\ref{eq:dyn-xs-corr-errors}).
The instruments above exhibit strong correlation under Anderson-Rubin tests and exclusion restriction has been derived.
However, there exists a niche case of diagonally-correlated errors,
namely despite (\ref{eq:dyn-no-ac}) and (\ref{eq:dyn-xs-corr-errors}),
I ought to consider the following:

\begin{equation}
    \mathbb E[\varepsilon_{i,t} \varepsilon_{j,t-k}]\neq 0,\qquad\exists i,j,k
\end{equation}
%
This is not as implausible as it may appear due to this analysis' peer effects setting. 
There may be some local omitted shock $\nu_t$,
that impacts peer firm A's TFP via $\varepsilon_t$
and impacts target firm B's TFP after a lag via $\varepsilon_{t-k}$.
This could be a price shock with random adjustment \todo{or}
\par
Additionally, relaxing (\ref{eq:dyn-no-ac}) to allow for weak error autocorrelation
between limited $\varepsilon_{i,t}$ and $\varepsilon_{i,t-k}$ for limited $k$
proves a similar problem.
\par
\todo{Firstly, I test for error autocorrelation.}

\subsection{Panel vector autoregression}

Another mechanism offered by this dynamic setting is
to extremely flexibly introduce additional lags of my peer effect term,
$\sum_{j\neq i} w_{i,j,t}z_{j,t}$.

\begin{align}
    y_{i,t} &= \beta_0 + \alpha_i + \gamma_t
                + \sum_{k=1}^p \phi_k y_{i,t-k}
                + \beta_1 k_{i,t}
                + \beta_2 l_{i,t}
                + \sum_{k=1}^q \delta_q \sum_{j\neq i} w_{i,j,t}z_{j,t}
                + \varepsilon_{i,t}
\end{align}

The model now has the form of a panel VAR(p,q) with internal instruments,
where $p$ refers to the autoregressive lags on the dependent variable $y_{i,t}$
and $q$ refers to the regressor lags on $\Delta\sum_{j\neq i} w_{i,j,t}z_{j,t}$
chosen by my specification.
\par
Following above reasoning, lags of $\Delta\sum_{j\neq i} w_{i,j,t}z_{j,t}$
of a higher order than 2 are exogenous to my reduced-form specification.
However, 

\subsection{Alternative weights on peer TFP}

\begin{align}
    y_{i,t} &= \beta_0 + \alpha_i + \gamma_t
                + \sum_{k=1}^p \phi_k y_{i,t-k}
                + \beta_1 k_{i,t}
                + \beta_2 l_{i,t}
                + \sum_{k=1}^q \delta_q \sum_{j\neq i} w_{i,j,t}z_{j,t}
                + \varepsilon_{i,t}
\end{align}